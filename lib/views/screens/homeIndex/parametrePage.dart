import 'package:flutter/material.dart';

class ParametrePage extends StatelessWidget {
  const ParametrePage({ key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}